<h3>Search Results for: <%= params[:q] %></h3>
<p>Number of results: <%= FoodFacade.result_count(params[:q]) %> (displaying only 10)</p>

<% @foods.each do |food| %>
  <div class="food">
    <h4>Description: <%= food.description %></h4>
    <p>GTIN/UPC: <%= food.gtin_upc %></p>
    <p>Brand Owner: <%= food.brand_owner %></p>
    <p>Ingredients: <%= food.ingredients %></p><br>
  </div>
<% end %>
